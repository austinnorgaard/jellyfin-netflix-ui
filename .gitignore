bin/
obj/
*.user
*.suo
.vs/
.idea/
artifacts/
*.zip

# Netflix Sans is a proprietary Dalton Maag typeface licensed to Netflix.
# It is deliberately NOT redistributed here - the plugin fetches it at runtime
# only if the user explicitly opts in. Never commit these.
*.woff
*.woff2
*.otf
*.ttf

# CI workflow is kept locally until the gh token has the "workflow" scope.
# Add it with:  gh auth refresh -h github.com -s workflow  then remove this line.
.github/workflows/
